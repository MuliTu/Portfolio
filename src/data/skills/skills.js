import { languages } from './languages';
import { tools } from './tools';
import { versionControle } from './virsionControle';
import { stateManagement } from './stateManagement';
import { framework } from './framework';

export const skillsList = [
    {...framework},
    {...languages},
    {...tools},
    {...versionControle},
    {...stateManagement},
];
